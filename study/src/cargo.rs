fn main(){
    // cargo 或 cargo -h可以查看cargo信息
    //cargo --list可以查看cargo提供的所有命令

    // cargo build	编译当前项目
    // cargo check	分析当前项目并报告项目中的错误，但不会编译任何项目文件
    // cargo run	编译并运行文件 src/main.rs
    // cargo clean	移除当前项目下的 target 目录及目录中的所有子目录和文件
    // cargo update	更新当前项目中的 Cargo.lock 文件列出的所有依赖
    // cargo new	在当前目录下新建一个 cargo 项目

    //cargo help <command>显示命令的帮助信息

    //创建Rust项目
    //创建一个可执行的二进制程序，带main() cargo new project_name --bin
    //创建一个库，可以用cargo new project_name --lib

    //预编译项目可以自动将相关以来下载，也可以查看项目配置是否正确
}